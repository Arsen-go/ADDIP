const { roleAuthentication } = require("../auth");
const { User } = require("../models");

class AdminResolver {
  constructor() { };

  Query = { };

  Mutation = { };
};

module.exports = AdminResolver;