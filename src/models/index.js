const { User } = require("./user");
const { Attachment } = require("./attachment");
const { EmailToken } = require("./emailToken");
const { Admin } = require("./admin");

module.exports = {
     User, Attachment, EmailToken, Admin
};
